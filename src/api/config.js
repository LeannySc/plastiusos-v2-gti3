export const API_BASE_URL = "http://localhost:8080/api";

// ✅ Definimos esto para que no rompa el sistema
export const GTI_HEADERS = {
  "Content-Type": "application/json",
  // Aquí puedes añadir el bypass de ngrok si vuelves a activarlo después
};
